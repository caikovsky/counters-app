<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cornershop.counterstest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CounterApplication"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MainApplication"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.cornershop.counterstest.presentation.ui.splash.SplashScreenActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.cornershop.counterstest.presentation.ui.welcome.WelcomeActivity"
            android:theme="@style/Theme.CountersWelcome" />
        <activity
            android:name="com.cornershop.counterstest.presentation.ui.main.MainActivity"
            android:theme="@style/Theme.MainApplication" />
    </application>

</manifest>